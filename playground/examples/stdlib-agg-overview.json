{
  "title": "Stdlib: Aggregation helpers",
  "description": "Summaries with LENGTH/COUNT, SUM, AVG, MIN, and MAX.",
  "program": [
    "LET totalItems = LENGTH(input.items);",
    "LET totalAndCountSame = COUNT(input.items);",
    "LET totalCost = SUM(input.prices);",
    "LET averageCost = AVG(input.prices);",
    "LET lowest = MIN(input.prices);",
    "LET highest = MAX(input.prices);",
    "",
    "OUTPUT {",
    "    length: totalItems,",
    "    count_alias: totalAndCountSame,",
    "    sum: totalCost,",
    "    avg: averageCost,",
    "    min: lowest,",
    "    max: highest",
    "}"
  ],
  "input": {
    "items": [
      "pen",
      "pad",
      "bag",
      "pad"
    ],
    "prices": [
      9.5,
      12.0,
      4.75,
      14.0
    ]
  }
}